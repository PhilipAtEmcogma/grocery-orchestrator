{
  "_comment": "Ingestion source priority as first-class, reviewable data. This file declares WHICH recorded catalogue refreshes the serving table (Lineage A, grocery-products-dev) and in what order sources are preferred. It is config-as-data for the same reason freshness.json and feasibility.json are: 'the data team's collected catalogue is the primary input, the seed fixtures are the fallback' is a project decision that should be readable and version-controlled, not buried in an operator's PRICE_SOURCE environment variable.",
  "version": "1.0",

  "_the_decision_this_encodes": "Philip, 2026-08-29 (ADR 0003; infra/docs/08-OPEN-DECISIONS.md §1): the orchestrator SERVES from Lineage A (grocery-products-dev). Lineage B — the data sub-team's collected catalogue (smart-grocery-products-dev, ~3,000 offers across 10 Auckland stores, plus smart-grocery-recipes-dev) — is a raw upstream INPUT, transformed B→A by ingestion/lineage_b.py, never adopted as a serving table. This file makes the 'B is the primary input' half of that decision first-class: the data team's work is the primary source of real prices, and that is now recorded here rather than implied by an env var.",

  "sources": [
    {
      "name": "lineage_b",
      "role": "primary",
      "kind": "recorded",
      "description": "The data sub-team's collected catalogue under datasets/data/dynamodb_products, transformed B→A by ingestion/lineage_b.py. This is the real Auckland price data (Foodstuffs online catalogue, captured 2026-08-28 per datasets/DATA_SCHEMA.md). It is RECORDED data on disk: reading it makes no network request and does not touch the ACQUISITION-RISK.md §8 gate.",
      "selects": "LineageBSource"
    },
    {
      "name": "fixtures",
      "role": "fallback",
      "kind": "recorded",
      "description": "The curated seed fixtures (fixtures/products.json, ~150 items) the offline tests and the seed loader use. The safe default: small, hand-verified, one product per key. Used when the config's default_source names it, or when Lineage B is unavailable.",
      "selects": "FixtureSource"
    }
  ],

  "default_source": "fixtures",

  "_why_default_is_still_fixtures_not_lineage_b": "PRIORITY (which source is PRIMARY) and RUNTIME DEFAULT (which source resolve_source() picks when nothing selects one) are two different questions, and only the first is settled here. Lineage B is the primary INPUT — that is the recorded decision and the reason the real 2,759-row catalogue is what the serving table holds today. But promoting Lineage B to the automatic runtime default changes what the deployed ingestion Lambda does when invoked with no PRICE_SOURCE set, and that is a deployment behaviour change, not a documentation one. It is left as an explicit follow-up (see _promoting_lineage_b_to_default) so the change is deliberate and reviewed rather than a silent flip. Until then: the env var / explicit selection chooses Lineage B, exactly as the 2026-08-30 load did.",

  "_the_deployed_function_selects_lineage_b_explicitly": "DECIDED 2026-09-04 (Philip). The deployed ingestion function runs with PRICE_SOURCE=lineage_b set on the function, and scripts/build_lambda.py ships datasets/data/dynamodb_products in the archive so it has something to read. This is a DEPLOYMENT setting, not a change to default_source below: a laptop with no environment set still gets the fixtures, which is the right default there, and promoting the runtime default stays the separate reviewed change _promoting_lineage_b_to_default describes. Chosen that way round because the env var is reversible on the function without a release, and because the two questions -- 'what does a deployed refresh read' and 'what does an unconfigured run read' -- genuinely have different answers.",

  "_what_the_weekly_refresh_is_actually_for": "NOT FRESHNESS, and saying so plainly is the point. LineageBSource.CAPTURED_AT is the constant '2026-08-28' and datasets/DATA_SCHEMA.md describes the collection as a one-off snapshot, explicitly 'not real-time live sync'. So a refresh from lineage_b rewrites ~2,759 byte-identical rows carrying the SAME capture date: it cannot move the staleness horizon by one day, and a DAILY schedule doing that is the steady-state silence ingestion/guard.py exists to prevent (`added 0, changed 0, unchanged 2759`, forever, indistinguishable from a healthy refresh and from a broken one). The schedule was therefore moved from daily to WEEKLY and re-enabled as a LIVENESS CHECK, not a refresh. What it measures is the half of this system no offline gate can reach: EventBridge Scheduler still fires, Step Functions can still invoke the function, the ingestion role still holds Query/PutItem/BatchWriteItem on grocery-products-dev, the price-history table still exists and still accepts writes, and the normaliser still produces the same rows from the same source (a drift shows up as a non-zero `changed` count against data that did not change). Every one of those is a live fact that has already broken once in this project. It costs ~2,759 writes a week and appends nothing to price history, because history_pk + valid_date is the same key each run and the append overwrites the identical row.",

  "_woolworths_reports_zero_and_that_is_honest": "The Woolworths branch fetches 0 offers on every run: datasets/data/dynamodb_products holds five New World and five Pak'nSave store files and no Woolworths file at all. That is a data-coverage question (docs/OPEN-REVIEW-chain-coverage.md), not a fault, so LineageBSource returns an empty list rather than raising -- the raise is reserved for a MISSING catalogue directory, which means the archive is wrong. The weekly run therefore makes the two-chain gap visible as a recurring `fetched 0` beside two branches writing ~1,380 each, instead of leaving it as a claim in a review document.",

  "_this_default_no_longer_reaches_a_deployment": "AS OF 2026-09-03, default_source='fixtures' applies to LOCAL runs only. resolve_source() REFUSES to fall back to the fixtures when it detects a deployment (AWS_LAMBDA_FUNCTION_NAME or APP_STAGE set), because this default was reached by the deployed ingestion Lambda on a nightly schedule and wrote 152 hand-written fixture rows over the real catalogue in grocery-products-dev — where their product keys SHADOW the real ones, so a shopper asking for the cheapest milk got an invented price. Three separate fixture removals (2026-08-30, 2026-09-01, and the guarded loader added by PR #64) were each undone by the following night's run. A default is a statement about what is safe when nobody chose, and 'the demo catalogue' is only safe on a laptop. This does not change the value below, and it is not a substitute for _promoting_lineage_b_to_default: it makes an unmade choice fail loudly instead of quietly, so the promotion can stay a reviewed change rather than an urgent one. See ingestion/guard.py.",

  "_promoting_lineage_b_to_default": "To make Lineage B the runtime default, set default_source to 'lineage_b' HERE and confirm: (1) the deployed ingestion Lambda's datasets/ are present in the archive or mounted, (2) a dry-run refresh (event {\"retailer\": ..., \"dry_run\": true}) reports the expected added/changed/unchanged against the live table, and (3) reject_implausible finds nothing new. This is a config change, and config ships inside the Lambda archive (AGENTS.md), so it is a deploy. Do it as its own reviewed change with the dry-run evidence in the PR.",

  "_override": "PRICE_SOURCE in the environment still overrides default_source, for parity with the existing mechanism and so a one-off load can select a source without editing config. Precedence: LIVE_ACQUISITION=1 (refuses, always — the acquisition tripwire wins over everything) > PRICE_SOURCE env > default_source here > a refusal, if that default is 'fixtures' and the process is a deployment (see _this_default_no_longer_reaches_a_deployment). PRICE_SOURCE='fixtures' is explicit and is NOT refused anywhere: an operator who names the fixture catalogue has chosen it, and ingestion/handler.py::refresh still refuses to WRITE it over a table that holds the real one.",

  "_the_acquisition_tripwire_is_unchanged": "This file adds NO live-acquisition capability. Both sources are recorded data on disk. resolve_source() still raises NotImplementedError if LIVE_ACQUISITION=1, and that check runs BEFORE this config is consulted. A live retailer source remains Task 11.4, gated on all thirteen ACQUISITION-RISK.md §8 conditions, and nothing here relaxes that.",

  "_not_a_serving_table_switch": "This chooses which recorded catalogue INGESTION reads to refresh the serving table. It does NOT change which table the orchestrator serves from — that is always Lineage A (grocery-products-dev), selected by USE_DYNAMODB in src/handler.py, and unaffected by anything here."
}
