{
  "//": "SCAFFOLD — CDK app entrypoint + context. The stacks are stubs today; `cdk synth` will produce near-empty templates until lib/*.ts are implemented per infra/docs/03-STACK-SPECS.md.",
  "app": "npx ts-node --prefer-ts-exts bin/grocery.ts",
  "watch": {
    "include": ["**"],
    "exclude": ["README.md", "cdk*.json", "**/*.d.ts", "**/*.js", "tsconfig.json", "package*.json", "node_modules", "test", "docs", "manual"]
  },
  "context": {
    "//stage": "Default deployment stage. Drives the -dev suffix and production-mode flags (see lib/config.ts).",
    "stage": "dev",

    "//region-guard": "This project deploys ONLY to ap-southeast-2 (tech.md). bin/grocery.ts enforces it.",

    "@aws-cdk/core:bootstrapQualifier": "hnb659fds",
    "@aws-cdk/aws-iam:minimizePolicies": true,
    "@aws-cdk/core:checkSecretUsage": true,
    "@aws-cdk/aws-lambda:recognizeLayerVersion": true,
    "@aws-cdk/aws-s3:createDefaultLoggingPolicy": true,
    "@aws-cdk/aws-cloudfront:defaultSecurityPolicyTLSv1.2_2021": true
  }
}
